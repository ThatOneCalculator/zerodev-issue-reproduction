import { type Address, type Chain, type Client, type Transport } from "viem";
import type { KERNEL_VERSION_TYPE } from "../../../../types/kernel.js";
type AccountMetadata = {
    name: string;
    version: string;
    chainId: bigint;
};
export declare const accountMetadata: <TTransport extends Transport = Transport, TChain extends Chain | undefined = Chain | undefined>(client: Client<TTransport, TChain, undefined>, accountAddress: Address, kernelVersion: KERNEL_VERSION_TYPE) => Promise<AccountMetadata>;
export {};
//# sourceMappingURL=accountMetadata.d.ts.map